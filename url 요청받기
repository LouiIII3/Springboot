//컨트롤러는 변수를 사용할때는 중괄호 하나만 사용한다.
@GetMapping("articles/{id}")
  public String show(@PathVariable Long id) {
      log.info("id = " + id);
  //id를 조회하여 데이터 가져오기
  //모델에 데이터 등록하기
  //뷰 페이지 반환하기
      return "";
  }
}
